# notifications_ggsel

Сервис для обработки уведомлений о заказах с GGsel и отправки персональных уведомлений через Telegram-бота.

## 📌 О проекте

`notifications_ggsel` — backend-сервис на Python с использованием **FastAPI** и **aiogram**, который:

- принимает уведомления о новых заказах и проверяет параметры;
- отправляет администратору подробности заказов;
- запускает отправку проверочного кода игр Supercell~~~~ клиенту в зависимости от продукта;
- реализует Telegram-бота для взаимодействия с пользователем.

## 🚀 Функциональность

### 📬 API

| Метод | Путь | Описание |
|-------|------|----------|
| `GET /` | `/` | Тестовый маршрут, возвращает `welcome` |
| `POST /check` | `/check` | Проверка параметров заказа |
| `POST /notification` | `/notification` | Обработка полученного уведомления |

### 🤖 Telegram-бот

- Отправляет приветственное сообщение по команде `/start`.
- Используется для уведомлений администратора о событиях (новые заказы и т.п.).

## 🧠 Архитектура

Сервис состоит из:

- **FastAPI** — HTTP API;
- **aiogram** — Telegram-бот;
- Модулей для работы с базой, внешними API и отправки сообщений.

## 🛠 Установка

1. Клонируй репозиторий:

   ```bash
   git clone https://github.com/Cherimolah/notifications_ggsel.git
   cd notifications_ggsel
   ```
2. Создай виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
.\venv\Scripts\activate    # Windows
```

3. Установи зависимости:
```bash
pip install -r requirements.txt
```

4. Настрой переменные окружения в .env (по примеру .env.example).

📡 Запуск

Запустить сервер:
```bash
python main.py  # Started on port 8003
```